{
  "name": "worker-pro-refactor",
  "version": "11.5.5",
  "description": "Worker 智能中控 — 安全加固版 (XSS防护 · 信息泄露封堵 · 加密层优化)",
  "scripts": {
    "build": "node build.js",
    "dev": "wrangler dev --port 7890",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:verify": "node verify.js",
    "typecheck": "npx tsc --noEmit 2>&1 || echo \"(type errors expected — gradual migration)\""
  },
  "devDependencies": {
    "esbuild": "^0.24.2",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9",
    "wrangler": "^4.99.0"
  }
}
