{
  "name": "worker-pro-refactor",
  "version": "11.4.0",
  "description": "Worker 智能中控 — DNS自适应修复 · 加解密层清理 · 架构升级",
  "scripts": {
    "build": "node build.js",
    "dev": "wrangler dev --port 7890",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:verify": "node verify.js",
    "typecheck": "npx tsc --noEmit 2>&1 || echo \"(type errors expected — gradual migration)\""
  },
  "devDependencies": {
    "esbuild": "^0.24.2",
    "vitest": "^4.1.9",
    "wrangler": "^4.99.0"
  }
}
